<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Kế thừa config mặc định của Spring Boot -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- File log chính -->
    <springProperty scope="context"
                    name="LOG_FILE"
                    source="logging.file.name"
                    defaultValue="logs/social-be.log"/>

    <springProperty scope="context"
                    name="LOG_LEVEL"
                    source="logging.level.root"
                    defaultValue="INFO"/>

    <!-- 🎨 Console appender: giữ nguyên màu -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                ${CONSOLE_LOG_PATTERN:-%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint}
                %clr([%thread]){cyan} %highlight(%-5level)
                %clr(%logger{36}){yellow} - %msg%n}
            </pattern>
        </encoder>
    </appender>

    <!-- 🧱 JSON file appender CHÍNH (tất cả level, 1 file / ngày) -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- file đang ghi hiện tại -->
        <file>${LOG_FILE}</file>

        <!-- xoay file THEO NGÀY -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- mỗi ngày 1 file, ví dụ: social-be-2025-11-14.log -->
            <fileNamePattern>logs/social-be-%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- giữ 30 ngày -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app_name":"social-be","env":"dev"}</customFields>
        </encoder>
    </appender>

    <!-- Logger mặc định -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE_JSON" />
    </root>
</configuration>
